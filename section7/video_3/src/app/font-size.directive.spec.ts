import { FontSizeDirective } from './font-size.directive';

describe('FontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = FontSizeDirective;
    expect(directive).toBeTruthy();
  });
});
